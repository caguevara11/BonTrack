{
  "name": "bontrack",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "start": "npm --workspace web run start",
    "lint": "npm --workspace web run lint",
    "install:web": "npm --workspace web install",
    "reconcile": "npm --workspace web run reconcile"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
